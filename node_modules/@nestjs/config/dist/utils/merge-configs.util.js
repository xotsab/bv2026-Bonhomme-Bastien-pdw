import set from 'es-toolkit/compat/set';
/**
 * @publicApi
 */
export function mergeConfigObject(host, partial, token) {
    if (token) {
        set(host, token, partial);
        return partial;
    }
    Object.assign(host, partial);
}
