import { randomUUID } from 'crypto';
import { getConfigToken } from './get-config-token.util.js';
/**
 * @publicApi
 */
export function createConfigProvider(factory) {
    return {
        provide: factory.KEY || getConfigToken(randomUUID()),
        useFactory: factory,
        inject: [],
    };
}
